# Required: Fernet encryption key for API keys in database
# Generate with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
DEBATE_ENCRYPTION_KEY=<run-migrate_keys.py-to-generate>

# Optional: share models with all users (counts against quota)
# Uncomment and fill to enable shared access:
# DEEPSEEK_REASONER_API_KEY=sk-...
# KIMI_CODING_API_KEY=sk-...
# MIMO_V2_5_PRO_API_KEY=sk-...
# GLM_5_TURBO_API_KEY=sk-...
